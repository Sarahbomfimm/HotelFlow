rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAuthenticated() {
      return request.auth != null;
    }

    // Permitir leitura e escrita para todos autenticados
    match /{document=**} {
      allow read, write: if isAuthenticated();
    }
  }
}
